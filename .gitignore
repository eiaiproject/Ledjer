# Dependencies
node_modules
**/node_modules

# Build output
dist
**/dist

# Environment files
.env
.env.local
.env.*
!.env.example
!**/.env.example

# Cloudflare local dev secrets (contains real secrets — never commit)
.dev.vars

# OS files
.DS_Store
__MACOSX

# Logs
*.log

# Supabase
supabase/.temp
supabase/.branches
supabase/.env
supabase/functions/.env

# Turborepo
.turbo

# IDE
.vscode
.idea

# Husky hooks
.husky/

# CI / tooling cache
.eslintcache
.parcel-cache
.vite
.wrangler
**/.wrangler
coverage
apps/web/eslint-report.json
.pnpm-store/

# Load tests
load-tests/

# Playwright
apps/web/playwright-report/
apps/web/test-results/

# Pi agent / Impeccable (local-only design artifacts)
.impeccable

# Internal remediation/audit files (not for public consumption)
LEDJER_MASTER_REMEDIATION_PROMPT.md
AUDIT-AND-REMEDIATION.md
*.remediation.md

# Personal worklog
worklog.md

# Screenshots (binary bloat, not essential for development).
# Root-anchored so apps/docs (public docs site) is NOT ignored.
# NOTE: /docs/ intentionally NOT ignored — docs are tracked.
/docs/screenshots/

# Codex hooks (local paths, not portable)
.codex/hooks.json

# Local-only credential / scratch files (NEVER commit)
temporary.md
*.local.md
.env.local.md
supabase-credentials.*
graphify-out/

# SonarScanner local analysis artifacts
.scannerwork/
