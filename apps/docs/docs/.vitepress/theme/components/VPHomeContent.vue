<template>
  <!-- Override default theme: drop the vp-doc prose container + --vp-offset so the
       home content (e.g. security section) aligns with the hero/features grid. -->
  <div class="home-content">
    <slot />
  </div>
</template>
