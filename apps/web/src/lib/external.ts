/**
 * External links referenced from the app chrome.
 *
 * Single source of truth so URLs stay in sync across layouts (public
 * footers, dashboard sidebar, mobile drawer).
 */
export const TRAKTEER_URL = "https://trakteer.id/eiaiproject/gift";
