{
  "$schema": "../../node_modules/wrangler/config-schema.json",
  "name": "ledjer",
  "main": "worker/index.ts",
  "compatibility_date": "2026-07-07",
  "assets": {
    "directory": "dist",
    "binding": "ASSETS",
    "not_found_handling": "single-page-application",
    "run_worker_first": ["/api/*"]
  },
  "d1_databases": [
    {
      "binding": "DB",
      "database_name": "ledjer-production",
      "database_id": "37eff57a-d152-4235-8344-9379c7ad4cf2",
      // TODO: Create a separate preview D1 database via `wrangler d1 create --name ledjer-preview`
      // then replace the empty string below with the preview database ID.
      "preview_database_id": "",
      "migrations_dir": "worker/db/migrations"
    }
  ],
  "triggers": {
    "crons": ["0 3 * * *"]
  },
  // Routes managed via Cloudflare Dashboard > Workers > ledjer > Settings > Domains & Routes
  // Custom domain routes in wrangler conflict with Dashboard-managed zone-level routes.
  "vars": {
    "APP_ENV": "production",
    "APP_ORIGIN": "https://ledjer.id"
  }
}
